{
  "name": "ai-virtual-try-on",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p public && cp index.html public/ || true"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
